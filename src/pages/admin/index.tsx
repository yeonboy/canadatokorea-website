// This page intentionally left minimal.
// We route users to the legacy /review dashboard instead of this sample admin.
// Keeping a tiny component prevents Next.js from throwing a 404 on /admin if linked.
export default function DeprecatedAdminRedirect() {
  return null;
}
